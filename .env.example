VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Only needed to (re)generate src/lib/cardEffects.json via `npm run generate-effects`.
# Not used by the app at runtime — the precomputed JSON is committed to the repo.
ANTHROPIC_API_KEY=
VITE_TRUELAYER_CLIENT_ID=
VITE_TRUELAYER_REDIRECT_URI=
TRUELAYER_CLIENT_SECRET=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=
